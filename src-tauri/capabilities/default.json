{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the windows",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "allow-all-app-commands"
  ]
}
